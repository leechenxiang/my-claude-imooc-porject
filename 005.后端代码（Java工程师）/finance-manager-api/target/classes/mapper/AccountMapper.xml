<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.finance.manager.mapper.AccountMapper">

    <resultMap id="BaseResultMap" type="com.finance.manager.entity.Account">
        <id column="id" property="id"/>
        <result column="ledger_id" property="ledgerId"/>
        <result column="user_id" property="userId"/>
        <result column="type_id" property="typeId"/>
        <result column="name" property="name"/>
        <result column="icon" property="icon"/>
        <result column="color" property="color"/>
        <result column="initial_balance" property="initialBalance"/>
        <result column="current_balance" property="currentBalance"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="created_at" property="createdAt"/>
        <result column="type_name" property="typeName"/>
        <result column="type_icon" property="typeIcon"/>
    </resultMap>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT a.*, at.name as type_name, at.icon as type_icon
        FROM accounts a
        LEFT JOIN account_types at ON a.type_id = at.id
        WHERE a.user_id = #{userId}
        ORDER BY a.sort_order
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT a.*, at.name as type_name, at.icon as type_icon
        FROM accounts a
        LEFT JOIN account_types at ON a.type_id = at.id
        WHERE a.id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO accounts (ledger_id, user_id, type_id, name, icon, color, initial_balance, current_balance)
        VALUES (#{ledgerId}, #{userId}, #{typeId}, #{name}, #{icon}, #{color}, #{initialBalance}, #{currentBalance})
    </insert>

    <update id="update">
        UPDATE accounts
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="icon != null">icon = #{icon},</if>
            <if test="color != null">color = #{color},</if>
            <if test="typeId != null">type_id = #{typeId}</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM accounts WHERE id = #{id}
    </delete>

</mapper>